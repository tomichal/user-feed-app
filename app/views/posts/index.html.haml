- if can?(:create, Post)
  %h1 New Post
  = render "posts/form"

%h1
  - if signed_in?
    Posts by users you follow
  - else
    All public posts
%ul{class: dom_class(Post) }
- @posts.each do |post|
  %li{ id: dom_id(post) }= post.content