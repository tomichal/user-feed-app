defaults: &defaults
  adapter: postgresql
  encoding: unicode
  pool: 5
  username: root
  password:

development:
  <<: *defaults
  database: user_feed_app_development

test:
  <<: *defaults
  database: user_feed_app_test

production:
  <<: *defaults
  database: user_feed_app_production
